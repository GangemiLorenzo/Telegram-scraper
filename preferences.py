

# This range specify how many seconds should
# the script wait between one user add and the next
# The default is between 40 and 50 seconds
user_add_delay = {
    "min": 40,
    "max": 50
}

# This range specify how many seconds should
# the script wait when an error occurs during user adding
# The default is between 40 and 50 seconds
adding_error_delay = {
    "min": 40,
    "max": 50
}

# This range specify how many seconds should
# the script wait when an error occurs
# The default is 200 seconds
FLOOD_ERROR_DELAY = 200


# This configuration variable specifies how many users
# an account can add before it is switched to the next
# one in the list
MAX_USERS_MOVED = 20
